name: Conditions workflow
on: push
jobs:
  fail:
    skip-or-run:
    if: ${{ github.actor == 'octcat' }}
    runs-on:  ubuntu-latest
    steps:
      - run: echo "Hello"
        if: ${{ failure() }}
