name: Naming
on: push
jobs:
  print:
    name: Super Useful job
    runs-on: ubuntu-latest
    steps:
      - name: 
  skip-or-run:
    if: ${{ github.actor == 'octocat' }} # octocatアカウント以外は実行をスキップ
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello"