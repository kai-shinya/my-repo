name: Generate functions
on: push
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - run: printenv | grep '_FUNC'
        env:
          FORMAT_FUNC: ${{ format('{0}, {1}.', 'Hi', 'world') }}     # 「ell」を含むか
          JOIN_FUNC: ${{ join(github.event.*.html_url, '.') }}